# IndicPass -- development dependencies (linting, typing, tests, notebooks).
#
#   python -m pip install -r requirements/dev.txt
#
# Installs everything in base.txt as well.

-r base.txt

# --- tests -------------------------------------------------------------
pytest>=8.3.4
pytest-cov>=6.0.0

# --- lint / format / types ---------------------------------------------
ruff>=0.8.4
black>=24.10.0
mypy>=1.14.0
types-PyYAML>=6.0.12

# --- notebooks (notebooks/ directory) ----------------------------------
ipykernel>=6.29.5
jupyterlab>=4.3.4
